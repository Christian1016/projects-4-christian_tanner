# projects-4-christian_tanner
# Project 4: Multi-URL HTML Table Extractor

This Go program extracts HTML tables from multiple URLs and saves them as CSVs.

## How to Run

1. **Build the Docker image**:
   ```bash
   docker build . -t project4
